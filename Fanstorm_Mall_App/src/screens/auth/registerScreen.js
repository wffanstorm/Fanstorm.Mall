
import React from 'react';

import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
    Button
} from 'react-native';

const RegisterScreen = ({ navigation }) => {
    return (
        <Text>Register</Text>

    );
};

export default RegisterScreen
